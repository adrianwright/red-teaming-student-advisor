# Azure OpenAI Configuration (REQUIRED for PyRIT 0.10.0)
# Get these from your Azure Portal → Azure OpenAI resource

# Your Azure OpenAI endpoint (just the base URL, no /api/projects)
OPENAI_CHAT_ENDPOINT=https://your-resource.openai.azure.com/

# API key for authentication  
OPENAI_CHAT_KEY=your-api-key-here

# Model deployment name (e.g., gpt-4, gpt-4o, gpt-35-turbo)
OPENAI_CHAT_MODEL=gpt-4

# Azure region where your resources are deployed
AZURE_LOCATION=eastus2

# Optional: If testing an AI Foundry agent
AZURE_AI_AGENT_NAME=StudentAdvisor


# Azure Speech Service (OPTIONAL - for Demo 4 audio attacks)
# Get these from Azure Portal → Speech Service resource
# AZURE_SPEECH_REGION=eastus
# AZURE_SPEECH_KEY=your-speech-key-here


# Azure SQL Database (OPTIONAL - for enterprise memory backend)
# Get these from Azure Portal → SQL Database
# AZURE_SQL_DB_CONNECTION_STRING=Server=tcp:your-server.database.windows.net,1433;Initial Catalog=pyrit;Persist Security Info=False;User ID=your-user;Password=your-password;MultipleActiveResultSets=False;Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;
# AZURE_STORAGE_ACCOUNT_DB_DATA_CONTAINER_URL=https://your-storage.blob.core.windows.net/pyrit-data


# Application Insights (OPTIONAL - for monitoring)
# Get from Azure Portal → Application Insights resource
# APPLICATIONINSIGHTS_CONNECTION_STRING=InstrumentationKey=...;IngestionEndpoint=...


# Azure Resource Details (OPTIONAL - for reference)
# AZURE_SUBSCRIPTION_ID=your-subscription-id
# AZURE_RESOURCE_GROUP_NAME=your-resource-group
# AZURE_AI_SERVICES_ACCOUNT_NAME=your-account-name


# PyRIT Configuration (OPTIONAL)
# Uncomment to customize PyRIT behavior

# Memory database type: IN_MEMORY, SQLITE, or AZURE_SQL
# PYRIT_MEMORY_TYPE=SQLITE

# Custom database path for SQLite (default: ~/.pyrit/results/pyrit_duckdb.db)
# PYRIT_DB_PATH=/custom/path/pyrit.db

# Logging level: DEBUG, INFO, WARNING, ERROR
# PYRIT_LOG_LEVEL=INFO
